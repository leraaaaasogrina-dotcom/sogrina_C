#include <stdio.h>
int main()
{
    char str[]= "lera-spiking 'hello'";
    char *p = srt;
    int i=;
    if(p==a || p==e || p==i || p==o || p==u || p==y)
    {
        str[i]= *;
    }
    i++;
    while(p>=str)
    {
        printf("%c", *p);
        p--;
    }
    return 0;
}